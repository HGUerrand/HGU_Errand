<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xsi:schemaLocation="
        http://www.springframework.org/schema/beans https://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/context https://www.springframework.org/schema/context/spring-context.xsd
        http://www.springframework.org/schema/mvc https://www.springframework.org/schema/mvc/spring-mvc.xsd">

    <mvc:annotation-driven />

    <!-- 정적 리소스(/assets/**) 허용 -->
    <mvc:resources mapping="/assets/**" location="/assets/" />

    <!-- DispatcherServlet이 처리 못한 요청은 Tomcat 기본 서블릿에게 넘김 -->
    <mvc:default-servlet-handler />

    <!-- 너 패키지명 기준으로 스캔 -->
    <context:component-scan base-package="com.hguerrand"/>

    <mvc:interceptors>
        <mvc:interceptor>

            <!-- 로그인 필요한 URL -->
            <mvc:mapping path="/errand/**"/>
            <mvc:mapping path="/application/**"/>
            <mvc:mapping path="/chat/**"/>
            <mvc:mapping path="/admin/**"/>

            <!-- 🔥 로그인 없이 허용 -->
            <mvc:exclude-mapping path="/auth/login"/>
            <mvc:exclude-mapping path="/auth/signup"/>
            <mvc:exclude-mapping path="/auth/google"/>
            <mvc:exclude-mapping path="/assets/**"/>

            <bean class="com.hguerrand.errand.interceptor.LoginInterceptor"/>
        </mvc:interceptor>
    </mvc:interceptors>


    <bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="prefix" value="/WEB-INF/views/"/>
        <property name="suffix" value=".jsp"/>
    </bean>

</beans>